/*****************SELECT*****************/
                            // case "ORDER BY":
                            //     if(index2!=null){
                            //         if(DEBUG)print("**SELECT** '"+polecenie + "' FROM " + table + " WHERE " + index + " = '"+value+"' ORDER BY "+ index2  );
                            //         return ::QuerySQL(db.connect,"SELECT "+polecenie+" FROM " + table + " WHERE " + index + " = '"+value+"' ORDER BY "+ index2 ); 
                            //     }
                            //     else print("**~BAD SELECT SYTNAX~** IF YOU WAN'T USE 'AND' YOU MUST ADD ADDITIONAL PARAMETERS <WHERE> <VALUE_WHERE> - LIKE USAGE DOSN'T WORK");
                            // break;
                            // case "GROUP BY":
                            //     if(index2!=null){
                            //         if(DEBUG)print("**SELECT** '"+polecenie + "' FROM " + table + " WHERE " + index + " = '"+value+"' GROUP BY "+ index2  );
                            //         return ::QuerySQL(db.connect,"SELECT "+polecenie+" FROM " + table + " WHERE " + index + " = '"+value+"' GROUP BY "+ index2 ); 
                            //     }
                            //     else print("**~BAD SELECT SYTNAX~** IF YOU WAN'T USE 'AND' YOU MUST ADD ADDITIONAL PARAMETERS <WHERE> <VALUE_WHERE> - LIKE USAGE DOSN'T WORK");
                            // break;
                            // case "ASC":
                            //     if(index2!=null){
                            //         if(DEBUG)print("**SELECT** '"+polecenie + "' FROM " + table + " WHERE " + index + " = '"+value+"' ORDER BY "+ index2 +" ASC" );
                            //         return ::QuerySQL(db.connect,"SELECT "+polecenie+" FROM " + table + " WHERE " + index + " = '"+value+"' ORDER BY "+ index2 +" ASC" ); 
                            //     }
                            //     else print("**~BAD SELECT SYTNAX~** IF YOU WAN'T USE 'AND' YOU MUST ADD ADDITIONAL PARAMETERS <WHERE> <VALUE_WHERE> - LIKE USAGE DOSN'T WORK");
                            // break;
                            // case "DESC":
                            //     if(index2!=null ){
                            //         if(DEBUG)print("**SELECT** '"+polecenie + "' FROM " + table + " WHERE " + index + " = '"+value+"' ORDER BY "+ index2 +" DESC" );
                            //         return ::QuerySQL(db.connect,"SELECT "+polecenie+" FROM " + table + " WHERE " + index + " = '"+value+"' ORDER BY "+ index2 +" DESC" ); 
                            //     }
                            //     else print("**~BAD SELECT SYTNAX~** IF YOU WAN'T USE 'AND' YOU MUST ADD ADDITIONAL PARAMETERS <WHERE> <VALUE_WHERE> - LIKE USAGE DOSN'T WORK");
                            // break;